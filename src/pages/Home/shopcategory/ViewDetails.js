import React, { } from 'react';
// import { useLoaderData, useParams } from 'react-router-dom';
// import SingleDetail from './SingleDetail';

const ViewDetails = () => {

    // const details = useLoaderData();

    // // const { img, sellerName, email, toyName, subCategory, price, availableQuantity, rating, description
    // //     } = _id;

    
    // const { id } = useParams();
    // const [detail, setDetail] = useState([]);

    // useEffect(() => {
    //     const matchData = details.find(detail => detail._id === parseInt(id));
    //     if (matchData) {
    //         setDetail(matchData)
    //     }
    // }, []);

    // console.log(detail);


    return (
        <div>
            {/* {
                details.map(detail => <SingleDetail
                key={detail._id}
                detail={detail}
                >

                </SingleDetail>)
            } */}
        </div>
    );
};

export default ViewDetails;